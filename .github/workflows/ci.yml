name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Swift build
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - run: swift build -c release
